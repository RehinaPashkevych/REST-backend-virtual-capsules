<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Capsule Collection</title>
    <style>
        .capsule {
            width: 100px;
            height: 50px;
            background-color: #ddd;
            border-radius: 25px;
            display: flex;
            justify-content: center;
            align-items: center;
            margin: 10px;
            box-shadow: 0 4px 8px rgba(0,0,0,0.1);
            transition: transform 0.3s ease;
        }

        .capsule:hover {
            transform: scale(1.1);
        }

        .container {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
        }
    </style>
</head>
<body>
    <div class="container">
        {% for capsule in capsules %}
            <div class="capsule"><a href="/capsule/{{ capsule.id }}">{{ capsule.name }}</a></div>
        {% endfor %}
    </div>
</body>
</html>

